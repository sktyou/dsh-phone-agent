<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        Normal permission: declared only, no runtime prompt. Without it
        killBackgroundProcesses() throws a Permission Denial, which made stopapp
        dead code until this line existed.
    -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <!--
        Android 10+ blocks background activity starts. Holding SYSTEM_ALERT_WINDOW
        is one of the platform's exemptions, which is what lets `launch` work while
        the agent itself is in the background — the normal case, since nobody is
        looking at this app when they ask it to open something else. It must be
        declared here before `appops set ... SYSTEM_ALERT_WINDOW allow` can take
        effect; without the declaration the grant silently stays "default".
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!--
        Android 11+ filters package visibility by default: without this, a query
        only returns packages the system considers relevant to us, which silently
        hides most of what is installed. Declaring it is what makes "list the
        phone's apps" actually answer the question. It is a manifest-only
        permission and grants no access to app data.
    -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HelpActivity"
            android:exported="false"
            android:label="功能帮助"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".adskip.AdSkipActivity"
            android:exported="false"
            android:label="自动跳过广告"
            android:theme="@android:style/Theme.Material.Light.NoActionBar"
            android:parentActivityName=".MainActivity" />

        <!-- stopWithTask=false: swiping the app off Recents must not stop the
             control channel, or the phone silently disappears from the PC. -->
        <service
            android:name=".ControlServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Device-owner local automation control channel" />
        </service>

        <service
            android:name=".AgentAccessibilityService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/agent_accessibility_config" />
        </service>

    </application>

</manifest>
